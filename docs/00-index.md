# 00 — Index: Mad Doctor's Construction Set Design Suite

The design documentation for **Mad Doctor's Construction Set (MadDr.MCs)** — a real-time strategy game about building movie monsters. Start with [01-vision.md](01-vision.md), then [02-gameplay-overview.md](02-gameplay-overview.md); after that, read by interest.

## Document map

| Doc | Purpose | Status |
| --- | --- | --- |
| [01-vision.md](01-vision.md) | Pitch, fiction frame, the four pillars, influences, what this is NOT | Draft |
| [02-gameplay-overview.md](02-gameplay-overview.md) | Core loops, match anatomy, victory conditions, the Menagerie, FTUE sketch | Draft |
| [03-mana-system.md](03-mana-system.md) | The Lumen Cycle, emitter polarities, affinity auras, capture rules — concrete numbers | Draft |
| [04-combat-model.md](04-combat-model.md) | Stat block, the damage formula, positioning/facing, bounded luck, salvage | Draft |
| [05-component-economy.md](05-component-economy.md) | Blood/Bones/Parts/Brains: sources, sinks, reanimation costs, anti-snowball | Draft |
| [06-mutator-design.md](06-mutator-design.md) | **The genome schema (normative)**, brain budget, **the Workshop**: Mutate/Splice/Graft/Megabrain Augmentation/Cannibalize, resource-driven construction | Draft |
| [07-mutator-server-architecture.md](07-mutator-server-architecture.md) | The cross-device Mutator service: data model, API, commute mode, scaling | Draft |
| [08-creature-visualization.md](08-creature-visualization.md) | Genome → 3D: socketed parts, blend shapes, uber-shader, perf budgets | Draft |
| [09-multiplayer-architecture.md](09-multiplayer-architecture.md) | Server-authoritative netcode, mobile resilience, matchmaking, anti-cheat | Draft |
| [10-engine-evaluation.md](10-engine-evaluation.md) | Unity vs Godot vs Unreal — scored matrix, recommendation, validation spike | Draft |
| [11-roadmap.md](11-roadmap.md) | Risk-first phases from paper playtest to launch; de-scope ladder | Draft |
| [12-open-questions.md](12-open-questions.md) | Open questions with decide-by dates; append-only decision log | Living |
| [13-lens-review.md](13-lens-review.md) | Book of Lenses design review: uniqueness, fun, marketability ratings | Reviewed |
| [14-ip-licensing.md](14-ip-licensing.md) | Classic-monster IP analysis, public-domain timeline, authoring guardrails | Reviewed |
| [15-part-genetics.md](15-part-genetics.md) | How parts mutate & crossbreed yet stay recognizable; prototyped in [`/prototype/mutator/`](../prototype/mutator/) | Draft |
| [16-brains-behavior-command.md](16-brains-behavior-command.md) | Brain genes → behavior: commanders, loyalty, rebellion, berserk; prototyped in [`/prototype/mutator/command.py`](../prototype/mutator/command.py) | Draft |
| [17-factions.md](17-factions.md) | Factions as expression profiles: human army (issued tech, rout/rally), alien hive (biotech, queen cohesion); prototyped in [`/prototype/mutator/factions.py`](../prototype/mutator/factions.py) | Draft |
| [18-city-battlefields.md](18-city-battlefields.md) | Unity battlefield layer: 5 km match scale, procedural city generation, destructible buildings, the engagement-zone LOD scheme, Unity↔Lab integration | Draft |
| [19-citizens.md](19-citizens.md) | Civilian city population: age/body type, aggression & weapon access, sync-tier LOD; distinct from the Human Army faction | Draft |
| [20-harvest-and-repair.md](20-harvest-and-repair.md) | Citizen and vanquished-foe harvesting (Collection Stations, faction corpse salvage, hybrid-monster parts), resource-gated construction (Bones cost, Megabrain Augmentation), field Repair, and in-match Cannibalize | Draft |
| [21-world-upgrade-report.md](21-world-upgrade-report.md) | Unity battlefield presentation upgrade: world architecture report, sculpted terrain, 1950s building/road dressing, and its follow-up batches | Living |
| [22-economy-system.md](22-economy-system.md) | The living economy: per-unit onboard blood/bone/brain pools (capacities, damage spill, efficiency floors), medic + harvester units, storage structures (blood banks, bone piles), factories (the Stitchworks), fun-first/never-annoying design contract | Draft |
| [23-rts-master-build-plan.md](23-rts-master-build-plan.md) | **Execution plan** for the full-RTS expansion: three-faction combat + hybrid 4th category, themed bases & building rosters, Blood/Fuel/Ichor economy, RPG levels/gear/Fusion, flocking control, Lumen-clock time-of-day bonuses, roaming power-ups, NY/Paris/Montreal 1950s regions, Mafia-school graphics ladder, deterministic-lockstep 4v4 netcode. **§13 = four-expert panel review + accepted amendments.** | **Phase 1 implemented** (`packages/match-core`: `MatchState`/`PlayerState`/`FactionDef`/`Command`, fixed 10tps tick, canonical hash, supply cap + Chimera-Track-on-all-three-origins + entity-ID allocation per the §13 amendments; `Tools~/DetHarness` proves 10k-tick 8-player determinism); **Phase 1.5 sim-side done, Unity-side (opt-in) Phases A–C done, Phase 2 sim-side done** — deterministic unit movement, single-unit waypoint queueing, and separation (`SimUnit`, `CommandKind.MoveTo`/`MoveQueue`, `Flocking.Separate`, real pathing) ships sim-side, and [27-sim-view-migration-plan.md](27-sim-view-migration-plan.md)'s opt-in `SimBridge`/`SimUnitView` interpolated view (Phase A confirmed working in a real Editor session; Phases B/C verified numerically, Editor checks for queued moves and multi-unit separation specifically still outstanding) is implemented; group moves and alignment/cohesion (need an "order group" concept match-core still doesn't have) remain deferred. Phase 2 (bases & building roster): `BuildingDef`/`SimBuilding`/`CommandKind.BuildStructure` ship sim-side — placement validation, cost debit, construction lifecycle, destruction reopening hexes, the "full tech tree, deterministic, twice" acceptance bar. **The Unity half: done (2026-07)** — `BuildMenuHud.cs` (a hotkeyed 1-9 roster panel, cost-per-`ResourceKind` labels, unaffordable rows grayed via a new `SimBridge.PlayerWallet`), `BuildGhostCursor.cs` (mouse→hex via the existing `RuntimeCityBuilder.HexAt`, a live red/green preview from a new `MatchState.CanPlaceBuilding` query, left-click confirms via a new `SimBridge.QueueBuildCommand`, right-click/Escape cancels), and `BaseDresser.cs` (syncs a primitive-cube GameObject per live `SimBuilding`, scaling up translucent during `UnderConstruction` off a new `SimBuilding.TicksUntilComplete`, solid per-`BuildingKind` hue at `Complete`, darkened when `IsDamaged`, despawned at `Destroyed`) — all wired into the one scene bootstrap that creates a real `SimBridge` (`RuntimeCityBuilder`'s `simDrivenDemo` block), alongside `LumenHud` (which had been built but never actually wired into any scene until this same pass — a real gap, now closed). `MatchState.CanPlaceBuilding` is a genuine refactor, not a duplicate check: `ApplyBuildStructure` itself now calls it, so the ghost's preview and the sim's real outcome can never disagree. Deliberately out of scope for this slice, flagged rather than attempted: per-faction HQ skinning (docs/23 §2's own phrase — the HQ renders with the same generic per-kind hue, just Landmark-tier-sized), a per-kind ghost/building SHAPE (every kind is the same placeholder cube, only color/scale differ), destruction FX (a building just despawns, no rubble reuse from the existing `DamageFx` system), and multi-hex footprints (still a single-hex-only slice, matching `SimBuilding`'s own header). 246 match-core tests total (up from 239) — `CanPlaceBuilding` and `TicksUntilComplete` both real, tested APIs, not just plumbing. **Phase 3 (economy): wallet-cap enforcement done, sim-side** — `PlayerState.RaiseWalletCap`, applied the tick a storage building (`BloodStorage`/`FuelStorage`) completes, base cap left honestly uncapped (docs/22 §6's own Q28 gives no real number); every other Phase 3 task (income ticks, upkeep drains, onboard per-unit pools, the `FuelNodes` generator, gas-station dressing/HUD) is gated on real prerequisites that don't exist yet — flagged, not faked. **Phase 3.5 (emitters + Lumen mana currency): sim-side core done** (84 match-core tests, 152 citygen-core tests total) — `LumenPhase`/`LumenClock` (pure function of `Frame`), `SimEmitter` (automatic capture, contested-pause, ownership), `PlayerState.Mana` (disjoint currency, cap 100), and citygen-core's new `EmitterPolarity` (Solar/Lunar/Twilight, round-robin assigned) all match docs/03's real v0.1 numbers exactly — no invented placeholders needed here, unlike Phase 3. A real, pre-existing gap surfaced while testing (not introduced by this phase): a landmark's own Site hex is part of its solid building footprint, so ground units can never actually stand on the exact hex docs/03's capture rule names — flagged in docs/12, not resolved. Unit affinity/`emitterMod`, the Dominion victory timer, and the Unity moon-dial/capture-bar/mana HUD remain deferred (need combat/genome data and a win-condition system that don't exist yet) — **the affinity half landed with Phase 4 below.** **Phase 4's combat-core amendment (damage formula + arcs + death/salvage window, moved up from Phase 6 per §13 amendment C): done, sim-side** (108 match-core tests total) — `CombatStats` as an optional `SpawnUnit` parameter (same genome-agnostic pattern as Speed/Radius), `CommandKind.AttackUnit` resolving docs/04's real formula exactly via citygen-core's existing `Facing.ArcOf` (melee posMod), Phase 3.5's aura infrastructure + a new per-unit Affinity (emitterMod), and the seeded `SimRng` (luckRoll/crit) — every multiplier an integer percent per docs/04's own stricter-than-usual determinism requirement. Death opens a 150-tick salvageable window (sim state only, no resource payout yet). Ranged posMod, turn-time-gated facing, chase-to-range movement, and actual salvage payout remain deferred from the combat core. **Phase 4's own RPG tasks: XP/Level done, sim-side** (127 match-core tests, 152 citygen-core tests total) — `UnitLeveling` ports docs/23 §4's real kill-XP formula, 10-entry level-threshold table, and linear per-level stat bonus (+8% MaxHP/+4% damage/+2% speed) exactly; `SimUnit.EffectiveMaxVitality`/`EffectivePower`/`EffectiveSpeed` apply the bonus on top of the untouched genome-derived base stats, already wired into `TickCombat`'s damage and `Tick`'s movement math; kill XP credits the attacker the instant a kill lands. Assist XP (no specified assist window) and building-destruction XP (no `AttackBuilding` command exists to credit) are deferred as real content/prerequisite gaps; **Trait choices are deferred because docs/23 only names 3 of the 9 required traits** — a genuine content gap, not a missing mechanism; Gear (needs the salvage system) and Fusion (needs Unity/genome-core rendering work plus an untracked Bones-cost stat) remain separate, not-yet-started slices — flagged in docs/23 §4's status note and docs/12, not faked. **Phase 5 (flocking control): alignment + cohesion done** (133 match-core tests, 152 citygen-core tests total) — `Flocking.Alignment`/`Cohesion` (pure math, un-wired into `MatchState`'s own tick loop per §5's own match-core/Unity task split) plus live Unity wiring in `MonsterSteeringController.Combine`, additive-only (existing separation terms/weights untouched; new terms are zero for any unit without same-`Faction` groupmates within 12m, verified directly via 3 regression checks in the `steercheck` harness, not just asserted). **Follow-up (2026-07) shipped: attack-move + patrol.** `MonsterAgent.OrderKind.AttackMove` (patrol reuses it via an `_isPatrolling` flag) auto-detours into a real `AttackUnit` fight on any enemy `NearestEnemyOf` spots en route, then resumes toward the original destination once the fight ends, via a `GoIdleOrContinueAttackMove` helper that's byte-for-byte `GoIdle()` for every other order kind; `WaypointCommander` binds `A`/`P` + left-click the same way `Ctrl+left` stands in for right-click; HUD hint line added to `HudStatus.cs`. No `SimDriven` equivalent (match-core has no sim-side attack-move concept) — a known, accepted gap. **Phase 6a (salvage drops), sim-side slice done** (147 match-core tests, 152 citygen-core tests total) — `SalvageMath` (docs/04's 40-60% uniform roll), `SimUnit.SalvageValue`/`SalvageRemaining` (genome-agnostic spawn parameter, same pattern as CombatStats), `CommandKind.SalvageCorpse` + a 3-second harvest channel paying the pile into the harvester's `Parts` wallet, re-validated every tick like combat's own Reach check. Fixed a real pre-existing bug: dead units were never filtered out of the separation pass, so corpses could drift. The 10% genome-fragment roll is sim-state-only (`YieldsGenomeFragment`) — match-core has zero reference to genome-core/the Mutator catalog by design, so naming a family/checking ownership/granting the +5% affinity or Lab-unlock is a real, separate, not-yet-built cross-system consumer job. **Phase 6a (roaming anomalies), sim-side slice also done** (157 match-core tests, 152 citygen-core tests total) — `SimAnomaly` (a separate lightweight entity kind, not a `SimUnit`), spawned at a `CityModel.Roundabouts` hex; `CurrentBuff(frame)` cycles Damage→Speed→Regen→XpGain every 20s as a pure function of its own spawn frame (same shape as `LumenClock`); `CommandKind.AttackAnomaly` + a separate `TickAnomalyCombat` loop resolve attacks (flat posMod, 0 Armor — no facing to flank); killing one grants the attacker that buff for 90s (docs/23's own real number) and respawns it elsewhere. All four buff magnitudes (+25% damage/+25% speed/5%-per-second regen/+50% XP) are invented v0.1 placeholders — docs/23 names the four buffs and the 90s duration but no actual numbers. Wander movement is explicitly deferred (no Citizen-walker exists in match-core to reuse) — an anomaly sits still between captures, still fully functional as a capture point. **Phase 6b (faction rosters), sim-side data table done** (169 match-core tests, 152 citygen-core tests total) — `UnitRosterDef` (mirroring `BuildingDef`'s own pattern) gives Human Army (Rifleman/Half-Track/Tank/Zeppelin Gunship) and Alien Hive (Drone/Spitter/Floater Queen) each a real `CombatStats`/Speed/Radius/SalvageValue block, plus `MatchState.SpawnRosterUnit` to field one; MadDoctor gets no roster (custom-bred creatures only). Every number is an invented v0.1 placeholder — match-core has zero reference to genome-core, so there's no bridge from docs/17's real expression math to this table; it exists so 6c's AI and Phase 7's balance test have real units, not a claim these are the actual bred genomes. **Phase 6c (skirmish commander AI) done** (193 match-core tests, 152 citygen-core tests total) — `SkirmishCommander` utility-scores every action a unit could take (attack / capture anomaly / strip corpse / advance / retreat / seize emitter) and issues the winner, with `ThreatMap` supplying the exposure term as an on-demand falloff function rather than a materialized grid. It is a **command source, not part of the tick** (reads state → returns `Command`s, exactly like a human input layer), so lockstep replicates its orders normally, replays never re-run it, and its `double` scoring can't threaten §0 float discipline; decisions are RNG-free and entity-ID-ordered, so AI-vs-AI is hash-identical run to run. `CommanderPersonality` is six axes in three tension pairs, dial-able two ways: **named archetypes + `.With(trait, value)`**, or **`Generate(seed)`** procedurally (each rolled commander gets a *signature* — one pair driven apart — so seeds produce characters, not a blur of 0.5s; decision cadence derives from Discipline). Build-order scripting is deferred: no unit-production command exists in match-core at all. **Phase 7 (Lumen Cycle faction modifiers) done, sim-side** (217 match-core tests, 152 citygen-core tests total) — `LumenClock`/emitter polarity already shipped with Phase 3.5; this phase adds `FactionLumenTable`, docs/23 §7's own six REAL numbers (not invented placeholders) wired three ways: Army's Day +15% damage into `CombatMath.ResolveDamage`'s new optional `lumenModPercent` term, Hive's Day -10% / Doctor's Night +10% speed into `SimUnit.Tick`'s new `speedMultiplier` param, and Doctor's regen swing into docs/06's real `regeneration` quirk (1% max-HP/s out of combat) via a new opt-in `HasRegenerationQuirk` flag — all backward-compatible defaults, every pre-Phase-7 call site unchanged. Army's vision-radius and Hive's Ichor-income numbers are recorded as data only (no fog-of-war system, no generic income tick exists in match-core). Acceptance met both ways: a golden 12-cell table test, and a scripted Army-vs-Hive duel that wins at Day and loses at Night with luck-noise eliminated (guaranteed crit + non-adjacent reach). **Phase 8 (New York/Paris/Montreal region presets) done, citygen-core only** (168 citygen-core tests total) — `CityRegion` enum + `Region` field on `CityPreset`/`CityModel`; `CityPreset.NewYork()` reuses `BigCity`'s own scale verbatim, re-skinned with real named landmark archetypes; Paris/Montreal's sizes are invented placeholders (docs/23 §8 gives no explicit figure for either) using their own real landmark names. New `RoadPattern.Boulevard` (Paris): the cardinal grid plus two diagonal avenues traced as fixed-hex-direction walks from the map center (provably straight in world space — a cross-product collinearity check, not an approximation), meeting at a grand l'Étoile roundabout placed deliberately ON their crossing — a documented exception to MainStreet's own "never on the arterial" rule, which stays untouched and is regression-tested. An ASCII-dump text-art rendering of all three regions is committed to `docs/23-balance/` for visual sanity review. Unity-side dressing (region-keyed BuildingDresser/RoadDresser branches, palettes, signature props, region picker) is explicitly deferred — citygen-core's own generator/skin split, no Unity design pass attempted. **Phase 9 (solidity/boundaries/destruction audit) done** (220 match-core tests, 168 citygen-core tests total) — `ContainmentFuzzTests.cs` drives the literal acceptance bar (200 units, 100k ticks, seed-driven random `MoveTo`/`MoveQueue`) plus a building-churn companion and a no-tunneling proof. **The 100k-tick run found and fixed a real bug**: `SimUnit.Tick`'s regular path-following was never independently re-validated against the blocked set (only `ApplySeparationPass`'s own nudge was) — many individually-valid separation nudges can accumulate into a lateral drift off the path's centerline until a later step clips an unchecked hex. Fixed with `SimUnit.RevertToSafePosition` plus a per-tick re-validation in `MatchState.Tick`. Unity's named movers were audited by reading (no Editor available): `MonsterAgent.cs`'s flying `FollowPath` had a real unclamped-step violation, fixed with a one-line magnitude clamp (a no-op at normal frame timing); `CaptureState.TickPull` (shared by MonsterAgent/Citizen) has a real violation left unfixed (no city/blocked-set reference at all today — a real interface change with no Editor to verify the fix still feels right); `Tank.cs` and `MonsterSteeringController.Combine` have narrower, lower-confidence risks documented but not touched; TrafficCar/Citizen movement/WaypointCommander are confirmed safe; destroyed-building footprint reopening is confirmed correct (one shared blocked-hex source of truth, no desync). **Phase 10 (graphics): sub-phases 1–2 done, Unity-layer only** — `LumenCycleController` (supersedes `NightMode.cs`) replaces the old binary day/dusk toggle with a continuous Dawn/Day/Dusk/Night cycle cross-faded from the same pure `LumenClock` math Phase 7 uses, driving a runtime-built URP Volume (color grading, film grain, vignette, bloom, ACES tonemapping — parametric per-region tint off `CityRegion`, a documented substitute for an authored per-region LUT texture this environment can't build) plus animated sun elevation/color (Day capped to a low angle for long cast shadows, per the 2026-07 daytime mood-board note) and a `StreetLampLightBudget` promoting the nearest-to-camera streetlamp bulbs to real warm point lights on a budget. **Sub-phases 3–4 also done, placeholder-generated rather than authored** — `PbrTextureAtlas` procedurally generates six small placeholder textures (brick/limestone/asphalt-wet/chrome/painted-metal/glass) wired into the existing dresser material functions with zero geometry changes; `PropLibrary` + `ProceduralMeshKit` ship the mesh-by-key/primitive-fallback infrastructure the plan asks for, backed by two hand-authored placeholder shapes (a tapered pole, a lean-to awning wedge) used for a new ornate multi-globe lamppost and market-stall prop in `RoadDresser`'s furniture rotation. **The mood-board's streetcar/tram-rail prop: done (2026-07)**, closing all three reasons the original deferral named — a moving vehicle (new `TramCar.cs`), a distinct embedded-rail street treatment (new `TramDresser.cs`, a different concept from the existing freight `RoadDresser.DressRailSiding`), and region-gating (New York only, per the mood-board's own consistent "likely NY" hedge — `RuntimeCityBuilder.SpawnTram` is a no-op for every other `CityRegion`). No per-hex district/region tag existed anywhere to key a tram line off of, and New York's own `Grid` road pattern has no distinguished-arterial subset either, so `TramDresser.TraceLine` computes a real route itself: the longest straight cardinal corridor of road hexes through downtown (the same offset-column/row walk the 2026-07 cardinal road rewrite established), capped at 40 hexes centered on the start — a real finding from verifying against actual generated cities: the UNcapped trace reached 250+ hexes (~5 km, an ~18-minute one-way trip nobody would ever see complete) before the cap was added. `TramCar` is deliberately much simpler than `TrafficCar` — no wander/park cycle, just the fixed path back and forth, pausing (not swerving — it's rail-bound) if a monster blocks the way ahead. Verified three ways, no Unity Editor available in this environment: (1) a stub-compile of the real `TramDresser.cs`/`TramCar.cs` against real citygen-core types and shape stubs, 0 errors/warnings (one real stub gap it caught: the harness was missing `Mathf`'s `int` overloads of Min/Max, a harness bug, not a code bug, per the lighting skill's own "suspect the harness first" guidance); (2) a standalone console check running the real trace logic against all six actual generated presets — New York traces a real, deterministic 39-hex/~780m/~3-minute line, every hex confirmed to be a real contiguous road hex; (3) that same check is what surfaced the 250-hex/18-minute finding above, worked through rather than shipped as-is. Not seen in a real render. Sub-phases 5–6 (creature normal maps/seams, FX upgrades) remain untouched; SSAO and light cookies are a similar Editor-asset-only gap within sub-phase 2. See `docs/23-balance/graphics-{1,2,3,4}-notes.md` for the full shipped/deferred split. Not visually verified (no Unity Editor in this environment); flightcheck-compiled only. **Phase 3.5's own deferred "Unity moon-dial/capture-bar/mana HUD" trio: done (2026-07)** — new `LumenHud.cs` (IMGUI, same layer as HudStatus/Minimap/AnalogClockHud) shows the live Lumen phase name with a 10-second pre-transition warning (the glossary's own "moon dial" definition), the local player's mana bar against its 100 cap, and a progress bar for every emitter currently mid-capture (`SimEmitter.CaptureProgressTicks`' own doc comment named this exact use). New match-core primitive backing it: `LumenClock.TicksUntilNextPhase(frame)` (239 match-core tests total, up from 220, boundary + wraparound covered) plus six new narrow `SimBridge` pass-throughs (`CurrentLumenPhase`/`TicksUntilNextLumenPhase`/`PlayerMana`/`EmitterCount`/`EmitterAt`, same defensive-default style as the existing `OrderOf`). World-space placement (a marker hovering over the actual emitter hex rather than a fixed panel) and the region picker are explicitly deferred as separate, larger follow-ups. Verified two ways, no Unity Editor available in this environment: (1) `LumenHud`'s pure formatting/threshold methods (`PhaseLabel`/`IsTransitionWarning`/`FormatCountdown`/`Fraction`/`FlashOn`) real-compiled and ran against match-core's actual built DLL — a genuine step up from earlier HUD work's "verified by reflection," since these touch no UnityEngine type at all; (2) a stub-compile of the real `LumenHud.cs` against a minimal UnityEngine stub + a signature-matched `SimBridge` shape confirmed the file (and its calls into the real `SimBridge` additions) compiles clean. **Follow-up (2026-07): capture bars moved world-space, region picker added.** `LumenHud`'s capture-progress bars now hover directly over the actual emitter hex in 3D space (`Camera.WorldToScreenPoint` + the GUI-space Y-flip) instead of a fixed on-screen list — the literal reading of docs/03's "progress bar visible to both players," the first HUD element in this project to project a 3D point at all; the fixed panel is now phase+mana only, fixed height. Separately, docs/23 Phase 8's own still-open "region picker" item is done: `RuntimeCityBuilder.PresetChoice` gained `NewYork`/`Paris`/`Montreal` (wired to citygen-core's own real `CityPreset` factories from Phase 8, their first Unity-side consumer), `Start()` was split into itself (unwinds the CityGizmo sync, then either shows a picker or proceeds) and a new public `BeginMatch()` (everything generation/spawn/HUD-wiring used to do unconditionally) — a new opt-in `showRegionPicker` flag (default off) gates a new `RegionPickerHud.cs` screen; off by default, every existing scene/workflow (Inspector preset field, CityGizmo) runs byte-for-byte the same `BeginMatch()` call it always did. Verified two ways, no Unity Editor available in this environment: (1) a stub-compile of the five real HUD/build scripts together (including the two changed/new this round) against real match-core/citygen-core types and signature-matched shape stubs, 0 errors/warnings; (2) manual re-read of every `RuntimeCityBuilder.cs` edit site (too large a file to stub-compile whole, same posture as every prior HUD/build entry) confirming the `Start()`/`BeginMatch()` split preserves the exact original call order with nothing dropped or reordered. Not seen in a real render. **Follow-up (2026-07): region picker gained live thumbnails.** `RegionPickerHud` now generates a real `CityModel` per option at a fixed preview seed (docs/18's determinism contract: same seed+preset = the same six thumbnails every time, not a reroll) and bakes it into a small top-down texture with `Minimap.BakeTerrain`'s own exact palette/stamp technique (water/ridge/road/building-tier/landmark colors, same `stampRadius` clamp), reimplemented against a standalone `CityModel` via `HexCoord.ToWorld()` directly since no live `RuntimeCityBuilder`/`_origin` exists yet at picker time. Hovering an option swaps the preview panel to that thumbnail. `RuntimeCityBuilder.ResolvePreset` was made `public static` (was a private instance method) so the picker can resolve any candidate preset without a live instance's `preset` field already pointing at it. Verified three ways, no Unity Editor available in this environment: (1) the same six-script stub-compile as the entry above, extended with `Texture2D`/`Mathf`/`RuntimeCityBuilder.ResolvePreset` stub surface, 0 errors/warnings; (2) a standalone console check running the REAL bake math against all six actual generated presets via the real citygen-core DLL — no crashes, non-degenerate bounds for every preset, and a determinism check (same seed baked twice, identical texel count); (3) that same check surfaced a real, honestly-recorded finding (not silently fixed or silently ignored): BigCity/NewYork read as ~100% texel coverage at this resolution — `stampRadius` was deliberately kept matching `Minimap.BakeTerrain`'s own proven clamp exactly rather than inventing a different one to chase a lower number, since whether that reads as a legible mosaic or a flat blob is a real visual question nobody has seen answered yet. **Follow-up (2026-07): a live component-wallet/supply HUD.** New `ResourceHud.cs` shows the real docs/05/§3 economy (Blood/Fuel/Ichor/Bones/Parts/Brains wallets, with cap when finite, plus Supply used/cap) — sim state real and tested since Phase 1 (supply) and Phase 3 (wallet caps) but never displayed anywhere; the build menu only ever previewed individual costs against it, never showed the standing balance. Deliberately a separate panel from `LumenHud`, not folded into it — docs/03's own framing keeps mana (energy) and components (material) as genuinely non-interchangeable currencies, and this panel follows that same split. Three new narrow `SimBridge` pass-throughs (`PlayerWalletCap`, `PlayerSupplyUsed`, `PlayerSupplyCap`), same defensive-default style as every other accessor. Verified via the same six-script stub-compile the other HUD/build entries use, extended with the three new `SimBridge` members — 0 errors/warnings. Not seen in a real render. **Follow-up (2026-07): real per-kind building silhouettes.** `BaseDresser`'s Complete-state visual used to be one uniformly-sized cube per building, distinguished only by an HSV hue keyed off `BuildingKind` — flagged by the maddr-aesthetic-preferences skill's own §5 ("shape communicates kind, color communicates state/owner, don't let one property carry both") as exactly the channel conflation it warns against. Now each of the seven buildable kinds gets a real two-primitive silhouette (BloodStorage/FuelStorage: a tank body + domed cap; FuelPump: a pump house + nozzle pole; PartsStorage: a warehouse body + roof vent; HarvestPost: a watchtower pole + platform; Factory: a body + offset smokestack; Defense: a bunker + turret dome; Hq: a keep + offset turret, Landmark-tier's own larger scale making it the biggest silhouette too) — all still primitive-kit only, no new mesh assets, matching every other dresser's own established constraint. Color now carries OWNER (a docs/17-flavored two-color approximation keyed by player index — `SimBridge` has no per-player `FactionId` accessor yet, so this isn't a true faction lookup, flagged rather than faked) plus the existing damaged-darkening, not kind. Verified via the same stub-compile as every build/HUD entry, extended with a handful of missing standard `UnityEngine` members the stub hadn't needed before (`Vector3.right`/`.forward`, `Mathf.Min`, `Transform.childCount`/`.GetChild`) — all real Unity API, harness gaps rather than code bugs, per the lighting skill's own "suspect the harness first" guidance. Not seen in a real render. **Follow-up (2026-07): worker-economy epic, Phase 1 (building HP/rubble/occupant-disgorge).** First slice of a larger causal chain the creator specced directly: destroyed buildings disgorge fleeing human occupants, a future Collector unit captures and possesses them into Worker units (SCV-style), and Workers become required to construct buildings, with three faction-specific production mechanics layered on top (Mad Doctor Big Brain control unit, Alien mind-control, Human recruiting) — later phases tracked as open tasks, not yet built. This slice: `BuildingDef`'s tier HP constants bumped 50% (Small 300→450/Medium 600→900/Large 1500→2200/Landmark 3000→4500, armor unchanged) and a new per-kind `Occupants` field (data only, not simulated — Factory=6 is the deliberately-sized figure feeding the rest of the chain, everything else a flat garrison loosely scaled by tier). Unity side: `BaseDresser`'s Destroyed branch (previously just despawned the GameObject with a comment flagging the exact gap) now fires once per building via a new `_destroyedHandled` guard, spawning a new `DamageFx.BuildingRubble` (lingering scattered debris chunks scaled off the building's own footprint, distinct from the existing one-shot `DustBurst`) and `def.Occupants` calls to a new `RuntimeCityBuilder.SpawnFleeingOccupant(HexCoord)`. Citizens spawned that way start in a new forced-panic-flee state (`Citizen.InitFleeingFrom`) — flees the wreck point directly for 4 seconds regardless of live monster proximity, inserted between the existing capture-override and proximity-flee checks so a future Collector can still capture one mid-panic — before falling back to normal AI. Verified with a real match-core `MatchState`/`SimBridge` driven through building placement→construction→destruction, then the real `BaseDresser.Update()` invoked via reflection over it: confirms exactly 6 Citizens spawn on destruction (not on Complete, not twice on repeated Update() calls) and that a disgorged citizen actually moves away from the wreck over simulated time. **That harness caught a real bug, not a harness gap**: a citizen spawned exactly on a hex's dead-center (every disgorged occupant, always) computing a 6m flee step landed inside the SAME hex (a hex's ~10m inradius exceeds the 6m step), so `HexAt`-snapping the flee target to its hex center rounded right back to the citizen's own starting position — it would never have moved for the entire forced-flee window. Fixed by using the raw flee point as the target instead of re-snapping to a hex center for this new forced-flee path specifically (the older proximity-flee block, which almost never starts from dead-center in practice, is untouched). All 246 match-core tests still pass. Not seen in a real render. **Follow-up (2026-07): worker-economy epic, Phase 2 (Collector + possess-into-Worker).** New `Collector.cs` (Tank.cs's pattern: a bespoke, non-genome `MonoBehaviour` with a plain `UnitCombat`, no weapon) seeks the nearest uncaptured `Citizen` and, once in range, calls `Citizen.Capture(combat, speed, possess: true)` -- reusing the EXACT SAME pull mechanic a web/psionic special attack already uses (`CaptureState`), just a new `possess` flag routing arrival to a new `RuntimeCityBuilder.OnCitizenPossessed` instead of the existing `OnCitizenEaten`. `OnCitizenPossessed` spawns a new `Worker.cs` (also Tank.cs-pattern: low-HP, no-weapon economic unit, capsule-plus-hard-hat silhouette) at the citizen's position and destroys the citizen -- no wallet credit, since the possessed worker IS the payoff, not a resource transaction. Citizen's own arrival branch (unchanged behavior when `possess` defaults to false) still eats for resources exactly as before -- verified by an explicit regression check, not just assumed. Neither unit is wired into any match-start spawn flow yet -- `RuntimeCityBuilder.SpawnCollector(hex)` is a real, tested, manual entry point (mirroring Phase 1's `SpawnFleeingOccupant` in status), flagged rather than faked: the real way a player should field a Collector ties into Phase 4's still-unbuilt Mad-Doctor production mechanic (the "Big Brain" unit / harvested-Brains cost). Verified with a reflection-driven simulation (same technique as the driving-verify/building-destruction-verify harnesses): a real `Collector` chases a real `Citizen` ~20m away, captures it, and it becomes exactly one real `Worker` within the simulated window; a parallel default-capture citizen still goes through the unchanged eat path. **That harness caught a real -- if narrow -- bug in the harness itself, not the shipped code**: the stub's `Object.Destroy` was a no-op, so repeatedly reflection-invoking `Citizen.Update()` after "destruction" kept re-firing the arrival branch (529 Workers instead of 1) -- real Unity stops calling `Update()` on a destroyed object the same frame `Destroy()` fires, so this never reproduces in actual gameplay; fixed by teaching the stub to track destruction and having the harness respect it, confirming the shipped `Citizen.cs`/`RuntimeCityBuilder.cs` code needed no change for this. Not seen in a real render. **Follow-up (2026-07): worker-economy epic, Phase 3 (worker-gated Factory construction).** Closes the epic's own "Workers required to construct buildings" link, scoped to Factory per the creator's exact words ("Factories are built by possessed human workers"). `BuildGhostCursor.cs`'s existing `CanPlaceBuilding` check gained `&& (!RequiresWorker(kind) || builder.Workers.Count > 0)` -- a new `RequiresWorker(BuildingKind)` policy method (Factory only, every other kind untouched), gating both the ghost's red/green tint preview and the left-click confirm identically to how an unaffordable/illegal placement already blocks. Explicitly, honestly a UNITY-SIDE-ONLY gate: match-core's own `ApplyBuildStructure`/`CanPlaceBuilding` have no concept of a Worker (Workers are a Unity-legacy-combat-layer unit, `Tank.cs`'s pattern, not a match-core `SimUnit`), so a build command sent directly through `SimBridge.QueueBuildCommand` bypassing this ghost cursor still succeeds with zero workers -- a real, flagged gap, not silently faked as sim-enforced; closing it for real needs either a match-core-side worker concept or Workers becoming actual `SimUnit`s, neither built here. Verified by reflection-invoking the new `RequiresWorker` policy method directly against every real `BuildingKind` value from the real match-core DLL (Factory true, all seven others false) -- the file's own `Update()` loop needs a real mouse/camera raycast to exercise end to end, judged out of proportion for a single boolean AND against an already-tested existing check. Not seen in a real render. **Follow-up (2026-07): worker-economy epic, Phase 4 (three-faction production).** Closes the epic's final link -- the shared `CommandKind.TrainUnit` production plumbing docs/12's Phase 6c entry flagged as missing ("no unit-production command exists in match-core at all"), plus a Mad-Doctor-specific mechanic. Shared infrastructure: `SimBuilding` gained a single in-progress training slot (`TrainingKind`/`TrainTicksRemaining`, `BeginTraining`/`TickTraining` -- v0.1 scope, ONE slot per building, not Stitchworks' 5-deep queue), included in `WriteTo`'s determinism hash; `MatchState.CanTrainUnit`/`ApplyTrainUnit` (same "one shared validity check, cost debited all-or-nothing before the slot opens" pattern as `CanPlaceBuilding`/`ApplyBuildStructure`) plus a `SpawnTrainedUnit` completion handler (nearest open hex to the producing building, ring-search fallback, matching the Unity-side `NearestOpenHex` twin from Phase 1); `UnitRosterDef` gained `Cost`/`TrainTimeTicks` fields. Human Army's existing roster (Rifleman/Half-Track/Tank/Zeppelin Gunship) costs Bones+Fuel (2 resource kinds each) -- "recruiting volunteers... require more resources," per the creator's own words; Alien Hive's (Drone/Spitter/Floater Queen) costs a single, heavier Ichor line -- "mind control... energy," their one native currency (the captured-citizen ENERGY SOURCE itself stays explicitly deferred, only the Ichor sink is real). Mad Doctor: since `FactionRoster.cs`'s own header already establishes the Doctor gets no fixed roster (bred creatures only), the "Big Brain control unit... controls 100 humans" is modeled as a new `BuildingKind.BigBrain` (20 Brains -- the one deliberately-sized, non-placeholder cost in the whole file) instead of a `RosterUnitKind`, reusing the existing `BuildingDef.StorageCapBonus`/`ApplyStorageCapBonus` pattern verbatim for a new sibling `SupplyCapBonus`/`ApplySupplyCapBonus` (+100 Supply, via `PlayerState.RaiseSupplyCap` -- existing, previously unused) rather than inventing a new mechanism. `SimBridge` gained `QueueTrainCommand`/`CanTrainUnit` pass-throughs, same contract as the existing build-command pair. 255 match-core tests total (up from 246) -- 9 new `TrainUnitTests` cover validation (Complete-only, single-slot, faction-match, affordability), all-or-nothing cost debit, completion spawn timing, and the BigBrain supply-cap mechanic including a no-double-apply regression check. Deliberately NOT attempted: AI wiring (`SkirmishCommander` never issues `TrainUnit`), a Unity-side training-queue HUD, and Collector/Big-Brain auto-spawn (Phase 2/3's own still-open gap -- a player still has no way to get their FIRST Worker without a Collector, which still has no spawn trigger). Not seen in a real render. **Follow-up (2026-07): traffic simulation gated by camera proximity.** Creator direction: "change where cars are driving to the areas close to or near the player view area. Let's not waste processing power." A car more than a new `RuntimeCityBuilder.trafficActiveRadius` (default 130m) from the camera now contributes ZERO per-frame cost -- `TrafficCar.Update()`'s very first line returns immediately (no threat scan, no roundabout/follow-distance math, no movement at all) until the camera comes back near it, at which point it resumes exactly where it was frozen, no teleport/pop. A new `RuntimeCityBuilder.RefreshTrafficActivity()` recomputes near/far for the whole fleet on a throttled 0.35s cadence (reusing `DynamicLightBudget.Refresh`'s own cadence and "cam == null: bail" convention, folded into the same shared `Update()` the existing traffic-band/deadlock-poll timers already use) rather than every car polling `Camera.main` itself every frame. Separately, `TrafficCar.PickNext`'s existing wander-hash scoring (the same scoring pattern that already penalizes hexes near a monster) gained a camera-distance term, so an ACTIVE car's route naturally curls back toward the view over a multi-hop trip instead of wandering off to a part of the map nobody's looking at -- a bias, not a hard override, tuned so it's comparable to the wander hash's own spread at the hex-to-hex comparison scale PickNext actually operates at (not the whole city span -- an initial weight tuned against city-scale distance was 5x too weak to move the needle at all, caught by the verification harness below). Verified via the existing driving-verify flightcheck (real `TrafficCar.cs` reflection-driven over simulated time): a car marked far from the camera provably never moves a single unit over 60 simulated seconds, then resumes driving the instant it's marked near again; the route bias was verified statistically across many junctions (not a single sample, since it's a nudge not a guarantee) -- moving the camera east measurably favors an eastward pick far more often than not. That same harness caught two real test-authoring bugs before landing on a valid check (both fixed in the test, not the product code): first, a straight 1-hex corridor scenario where `PickNext`'s own `_from`-exclusion rule leaves exactly one candidate at every hop past the first, so "final position after many hops" was silently measuring oscillation phase across repeated round trips, unrelated to the bias; second, comparing raw axial `HexCoord.Q` as a proxy for "east/west" across hexes with varying R, when world-X actually depends on both Q and R together (`HexCoord.ToWorld()`) -- produced a perfectly consistent but fully inverted result until fixed to compare real world position. Not seen in a real render. **Follow-up (2026-07): naturalistic driving -- speed variance + aggressive passing.** Creator direction: "Verify naturalistic driving. Speed up and some aggressive passing to slow cars." Two additions to `TrafficCar.cs`: (1) a personal per-car speed multiplier (`_personalSpeedMult`, 0.8x-1.35x of the base `CruiseSpeed`, hash-rolled at `Init` the same deterministic way every other per-car variance in this file already works) -- real 1950s traffic isn't one uniform speed, and this is also the actual precondition that creates a "slow car" for anyone else to catch up to in the first place; (2) a committed passing maneuver -- sustained close following (over `PassBlockedTriggerTime`) behind something genuinely slow (not just momentarily close) triggers an overtake: the car aims at a close lookahead point offset to the opposite side of the road (steep commit angle, not a shallow drift toward the far-off next-hex target -- see the verification note below for why that distinction mattered) at a boosted `PassSpeedBoost` (1.45x) speed, continuously re-checking the passing side is still clear every frame it's committed (abort back to normal following if it isn't, or if a safety-cap duration elapses), and merges back once its own original lane opens up ahead -- aggressive, not reckless. Verified via the existing `driving-verify` flightcheck, extended with a real (if simplified) `DistanceAhead` stub implementation (a verbatim port of the real method's dot-product/lateral-filter logic against an injected obstacle list, confirmed against `RuntimeCityBuilder.cs` directly) since the harness's prior stub hardcoded "always clear" and had nothing for the new follow/pass logic to react to. Confirmed: real per-car speed variance exists (8 cars covered a 1.36x spread over the same 20s unblocked); a car sustained-blocked by a stationary obstacle genuinely commits to passing, swerves a real ~3.3m wide of its own lane (clear of the own lane's own `LaneHalfWidth` filter, so it actually reads as having changed sides, not just wobbled), and gets past within a reasonable time rather than sitting behind it forever. **Two real issues the harness caught before landing, both instructive:** first, an initial pass-steering implementation offset the far-off next-hex target sideways by a fixed amount, which is a geometrically shallow angle over a long remaining distance -- measured out at only ~1-2m of actual lateral clearance before the maneuver naturally concluded, nowhere near a convincing "crossed to the opposite side" look; fixed by aiming at a close lookahead point recomputed every frame instead (a real product fix, not a test tweak). Second, and unrelated: the initial lateral-offset SIGN was backwards (`+right offset` pushed further into the car's own existing lane rather than across to the opposite side), caught by re-reading `RoadPoint`'s own `right`-vector convention directly, not by guessing. Not seen in a real render. **FIX (2026-07): traffic freeze regression -- wrong camera ground-focus point.** Creator report after the two follow-ups above: "still non of the cars are moving, in the editor." Root cause, confirmed by deriving the exact geometry rather than guessing: `RefreshTrafficActivity` was using `Camera.main.transform.position` directly (Y zeroed) as "where the player is looking," but `SimpleCameraRig.SnapTo` places the camera RIG itself at `focus + (0, height, -height*0.8)` -- well ABOVE and BEHIND the actual point it's looking at, never AT it. That's an error of exactly 0.8x camera height by construction (56m at the actual match-start height of 70) -- and since the default 10-car fleet spawns scattered across the WHOLE road network rather than clustered near the start camera position, this very likely froze the entire fleet from the first 0.35s refresh of every single match, not an occasional edge case. Fixed by raycasting from the camera through the VIEWPORT CENTER to the ground plane (y=0) -- the actual "what is the player looking at" point, the same question `SimpleCameraRig.FocusOn` already answers for its own G-key-jump feature via a near-identical ground raycast (reimplemented locally rather than adding a new cross-component dependency, since `RuntimeCityBuilder` only needs the math, not that component's own drag-state). Also switched the active radius from a single fixed guess (130m) to one derived LIVE from camera height every refresh, reusing `SimpleCameraRig.shadowDistancePerHeight`'s own already-proven ratio (1.9x height + a floor, capped) for "how far the visible ground extends from here" -- the wrong-point bug was the primary cause, but a camera-aware radius is far more robust across zoom levels than any one fixed number. Verified with a new pure-math flightcheck (no Unity dependency needed at all, since the bug and fix are both pure vector geometry) that replicates `SnapTo`'s EXACT camera placement and fixed 50-degree pitch: confirms the OLD approach really was off by 80% of camera height at every height and focus point tested (not a hypothetical), and the FIX recovers the true focus point to within ~4% of height (the small remaining residual is a pre-existing, harmless characteristic of `SnapTo`'s own fixed-pitch-vs-offset-ratio not being perfectly geometrically matched to each other -- confirmed separately, not something this fix introduces or needs to chase further). Also added a SAFETY NET alongside this fix: a new `trafficActiveMinimumCount` (default 12, just above the default 10-car fleet) always keeps the fleet's own nearest N cars active regardless of what the radius/focus calculation says -- the same "always promote the closest N" principle `DynamicLightBudget` already uses for real lights -- making it structurally impossible for a camera-geometry miscalculation (this one, or a future one) to ever freeze 100% of visible traffic again, and a genuine no-op for the default scene (every one of the 10 default cars always qualifies as one of the "nearest 12"). Not seen in a real render -- this is an Editor-reported bug none of this project's flightchecks caught in advance, since none of them can render a real camera/scene; recorded honestly as a real gap in this environment's verification ceiling (a flightcheck can prove the MATH is right once told what to check, but can't discover on its own that a feature silently breaks 100% of visible traffic in the actual game), not glossed over. **FIX (2026-07): the ACTUAL root cause of "cars not moving" -- an unguarded cosmetic-setup call could permanently strand the whole fleet.** Creator report continued after the camera-focus fix above didn't resolve it, then clarified: "this was happening before the camera addition" (ruling out that fix's own bug as the cause) and "the cars are just parked... I don't see any cars with light on either" (the concrete clue that cracked it). Reproduced with a genuine fault-injection flightcheck (not guessed): `TrafficCar.Init()` calls `BuildBody()` -- whose very first line is `new Material(ShaderUtil.FindRenderableShader())` -- BEFORE any of the state/route setup (`_state`, `_hopsRemaining`, `PickNext()`). Real `UnityEngine.Material`'s constructor throws if handed a null `Shader`; if `ShaderUtil.FindRenderableShader()` ever fails to resolve any of its three candidate shader names (a real, if unconfirmed-in-this-environment, possibility -- this environment has no Unity Editor to determine WHY it might, only that the code path exists and the flightcheck proves exactly what happens if it does), that exception aborts `Init()` before `_state`/`_target`/`PickNext()` are ever set up correctly. `_state` then reads its C# default (`Driving`, enum value 0) with `_target` still equal to the car's own spawn position and `_hopsRemaining` still 0 -- so the very first `Update()` reads that as "arrived, zero hops left" and calls `ParkHere()` immediately. Since `BuildLights()` (bulb array setup) never ran either (it's called right after `BuildBody()`, never reached), the car is left BOTH stuck in `Parked` (unable to ever redepart, since nothing set it up with a real trip) AND with no working lights, in one single failure -- exactly the reported symptom pair. Fixed by wrapping both `BuildBody()` and `BuildLights()` in `Init()` independently, so a fault in either purely-cosmetic call degrades gracefully (drives without a proper chassis material / without working lights) instead of silently aborting the rest of setup; also made `SetBulbsActive` null-safe and wrapped `UpdateLights()` itself defensively, since it's called from the very TOP of the Parked branch (before the park-timer/redeparture check) and any future fault there would have reproduced the exact same "stuck forever" failure mode by a different path. Verified with a genuine fault-injection flightcheck extension: the stub's `Material` constructor now reproduces Unity's own real "throws on a null Shader" contract, and a new `Shader.SimulateUnavailable` toggle forces the exact failure this hunts for -- confirms a car with this fault STILL ends up correctly `Driving` with a real `PickNext` target, still actually drives (not frozen), and a forced-`Parked` car still redeparts on schedule despite the ongoing fault every single frame. The same defensive wrapping was also added to `TramCar.Init()`'s equivalent `BuildBody()` call for consistency, though that one was confirmed structurally safe already (its own critical state is assigned before `BuildBody()` runs, so this was extra insurance, not a fix for an actual bug there). Not confirmed against a real Unity Editor -- this is the best diagnosis obtainable from source review plus a reproducing flightcheck in an environment with no Editor access; if cars still don't drive after this, the Console window's actual error text (if any) is the one remaining signal this project has no other way to obtain. **FIX (2026-07): the likely REAL root cause of the entire "cars not moving" saga -- 57 of 59 scripts had no committed `.meta` file.** Chasing a follow-up report ("the empty gameobject with the citybuilder is gone") surfaced something much bigger than a scene-sync issue: every script this project has added except `CityGizmo.cs`/`HexGridGizmo.cs` (and the two stock template scripts) -- including `RuntimeCityBuilder.cs` itself -- had never had a `.meta` file committed to the repo (not `.gitignore`d, simply never created, since scripts here are written directly without a real Unity Editor available to auto-generate one). `SampleScene.unity`'s `CityBuilt` GameObject stores a specific `guid` for its `RuntimeCityBuilder` component that has never had ANY committed backing -- meaning on any fresh clone or `Library` reset, Unity has no choice but to mint a brand-new, different guid for that script, breaking the scene's reference into "Missing (Mono Behaviour)." A missing script reference means Unity never calls `Start()` at all -- so `BeginMatch()`, `SpawnTraffic()`, city generation, all of it, would simply never run, independent of whatever fix landed in `TrafficCar.cs` itself. This is a complete, sufficient explanation for BOTH "the cars are just parked" and "the citybuilder is gone," and also explains why an EARLIER session already had to commit a "Restore creator's CityBuilt test GameObject" fix once before -- the actual gap (no committed `.meta` files) was never addressed, only that one symptom patched over. Fixed by creating `RuntimeCityBuilder.cs.meta` with the EXACT guid the scene has always referenced (confirmed by grepping the scene file directly), plus fresh, collision-checked guids for the other 56 scripts -- verified zero other assets/folders are missing a `.meta` and zero duplicate guids exist anywhere in the project. Not confirmed against a real Editor (none available here), but this is a structural, provable gap, not a guess. **Traffic routing overhauled (2026-07):** `TrafficCar` now picks a real `_destination` hex per trip (mirroring `Citizen.cs`'s own destination-walk pattern) and greedily favors whichever road neighbor gets closer to it, instead of a pure wander hash — monster-avoidance stays a safety override, camera-proximity bias moved from a per-hop term to the one-time destination pick itself, trip completion is now primarily "arrived at the destination" with the old hop-count budget demoted to a safety cap (see [12](12-open-questions.md)'s "destination-based traffic routing" entry). Along the way this also caught and fixed a second, differently-triggered instance of the passing-maneuver's "stuck forever, no net progress" bug from the previous log entry (`ownLaneClear` now tracks the car's own actual swerve displacement via `_passStartPos` instead of assuming a fixed offset). **FIX (2026-07): parked cars sat at an angle across the road, worst at corners** — `TrafficCar.ParkHere` oriented off the raw hex-to-hex direction instead of the same cardinal-corrected road anchor `RoadPoint` already uses for driving; now shares that correction via a new `CardinalAnchorOf` helper (see [12](12-open-questions.md)'s "cars parking diagonally across roads" entry). **2026-07: faction picker + `FactionId.Mixed` + starting Factory** — creator-directed ("player first must choose faction... or the mixed"; "give the player one fully functional factory on startup"). §1's "not a fourth lobby button" ruling is superseded, not silently contradicted (see the §1 update note): Mixed is now a real starting pick, gated behind a persistent "won a campaign" unlock (`MixedFactionUnlock`, honestly flagged — no campaign mode exists in this repo yet, so it stays locked until one calls `MarkUnlocked`), and every fielded unit resolves ITS OWN race's Lumen bonuses/handicaps (`SimUnit.RaceOverride`) rather than a stacked Mixed-wide bonus (`FactionLumenTable`'s Mixed row stays neutral). `MatchState.SpawnFactoryForPlayer` (mirrors `SpawnHqForPlayer`) closes the worker-economy epic's bootstrapping gap for the one starting Factory per player; `RuntimeCityBuilder.BeginMatch` now creates a real `MatchState`/`SimBridge` and wires the build-menu/ghost-cursor/BaseDresser/resource HUDs unconditionally (previously gated behind the dev-only `simDrivenDemo` toggle), not just when that toggle happens to be on. `FactionPickerHud.cs` copies `RegionPickerHud`'s exact opt-in wiring pattern (`RuntimeCityBuilder.showFactionPicker`, off by default). 263 match-core tests green, `Tools~/DetHarness` re-verified deterministic after the hash-format change (`SimUnit.RaceOverride` is now hashed). **2026-07: `BuildingNavHud.cs`, an SC2-style building nav bar** — creator-directed ("icons that quick navigate to my building... click... hilite... quick scroll... `<>` keys jump to the next building of that type"). One icon per Complete building this player owns, bottom-center, sorted by (Kind, EntityId); click highlights + glides the camera via the existing `SimpleCameraRig.FocusOn` (the same method the G-key jump-to-nearest-unit already uses); while highlighted, comma/period (`<`/`>`, unshifted) or two on-screen arrow buttons below the icon page through every OTHER building of that same Kind, wrapping both directions. Extends the `Minimap.PointerOver`/`BuildMenuHud.PointerOverPanel` click-guard pattern with a new `BuildingNavHud.PointerOver`, wired into both `WaypointCommander` and `BuildGhostCursor` — amendment H's own "IMGUI already forced the `Minimap.PointerOver` hack and won't survive build menus/toasts/dial" warning is still exactly as true as it was when written; this is one more instance of that same flagged debt, not a new kind of one. **2026-07: idle monsters auto-eat nearby citizens** when combat finds nothing to engage (`MonsterAgent.AcquireTarget`'s existing idle fallback, extended -- deliberately not gated behind the weapon-check, since eating never depended on one). **2026-07: `GrabCursor.cs`, G-key grab-and-clone** -- G (moved off the pre-existing camera-jump-to-nearest-unit, now J) toggles a claw cursor (procedurally-drawn, no asset files); click a monster to pick it up (wiggles/squirms via a new `MonsterAgent.TickHeld`), drop it on the player's own Factory to clone it, spending a flat v0.1-placeholder Blood cost per clone (`RuntimeCityBuilder.TrySpendBlood`, gated, unlike the existing unblockable `SpendWalletForCast`) up to a 10-per-drop cap. Not routed through match-core (the Mad Doctor has no fixed roster to hang a `TrainUnit`-style command off of) -- a real, flagged scope boundary, not an oversight. Phase 11 not started |
| [25-monster-movement-steering-plan.md](25-monster-movement-steering-plan.md) | **Approved migration plan**: replace the Unity-side monster collision reaction (ApplySeparation + AvoidanceDir) with hybrid steering (seek/separation/predictive avoidance/speed modulation) plus a deadlock-recovery fallback, phased A–E, each independently testable | Phases A–D implemented; Phase E (cleanup + tune) not started |
| [26-special-attacks-system.md](26-special-attacks-system.md) | **Approved architecture**: modular, cooldown-gated special attacks for enemy AI (ScriptableObject definitions, per-unit instances, Mass-based target classification), worked example Arachnid Web Attack (capture/pull human-scale targets, slow heavy ones), phased 1–8 + Phase 9 (secondary attacks for all races) + Phase 10 (cast cost + Lab display) | **All 8 phases + Phases 9–10 implemented**: every Tank and every monster is now actually equipped (Tanks get a damage-only Flamethrower; alien-tech-handed monsters get a Psionic Tractor Beam, the same PullAndConsume mechanic as Web Attack at shorter range; every other monster gets a new Stun-effect Ground Stomp) via runtime `ScriptableObject.CreateInstance`, no Editor step needed; every cast draws Blood+Bones from the session wallet (soft, never blocks the cast — docs/22's "floors, not stalls" contract, so a caster never truly runs dry); the same classification is now mirrored in a new `genome-core/src/attacks.ts` twin and shown in the Lab's vital-signs panel AND the Chop Shop's slab label (chop-shop-safe: a stump/grafted hand reads correctly with no special-casing); a web-captured citizen credits the harvest tank the same as a direct chase-and-eat kill (2026-07 follow-up); remaining follow-up (not part of this plan): non-Citizen consume path designed not built |
| [27-sim-view-migration-plan.md](27-sim-view-migration-plan.md) | **Approved architecture**: the Unity-side half of docs/23 §13 amendment A — how `MonsterAgent` cuts over to rendering interpolated `match-core` sim state instead of deciding movement itself, one order kind at a time (Move first), never a single blind rewrite of the whole ~950-line file | **Phases A–C implemented.** Phase A: `SimBridge` fixed-tick pump + command queue, `SimUnitView` interpolated render + measured-delta velocity, `MonsterAgent.EnableSimDriven`/`OrderMoveViaSim`/`TickMoveViaSim`; opt-in only; **Editor-confirmed (2026-07)** via the `simDrivenDemo` toggle on `RuntimeCityBuilder`. Phase B: single-unit waypoint queueing only (`CommandKind.MoveQueue`, `SimUnit._waypointQueue`, `SimBridge.QueueWaypointCommand`) — group moves (multi-unit formations/settle/`GroupFacing`) explicitly deferred. Phase C: sim-side separation only (`Flocking.cs`, `SimUnit.Radius`, `MatchState.ApplySeparationPass` with a blocked-hex clamp), closing the accepted "ApplySeparation must skip a sim-driven unit" gap by gating that ONE existing Unity call site (`&& !SimDriven`) — alignment/cohesion deferred, same "no order-group concept yet" reason as Phase B's group moves; `MonsterSteeringController.cs`, `RuntimeCityBuilder.ApplySeparation`'s own body, `Tank.cs`, `HexPathfinder`, and `BattlefieldState` are untouched, confirmed via `git diff --stat`, so legacy (non-sim-driven) navigation/avoidance is unchanged. `WaypointCommander` needed zero changes across all three phases; verified by 30 match-core tests + a 17-check standalone numeric harness + flightcheck, but **no Editor check has been run yet for queued moves or for multi-unit separation** — only Phase A's plain single-destination move has a live confirmation so far |
| [28-city-lighting-system.md](28-city-lighting-system.md) | City lighting architecture: a two-tier model (unlimited free emissive glow + one shared budgeted pool of real dynamic lights across every light kind combined) so streetlamps/windows/neon/marquee can scale to hundreds on screen; `CityLightingProfile` ScriptableObject for Inspector tuning; `EmissiveAnimator` batched flicker/buzz/chase via `MaterialPropertyBlock`, no per-object Update() | **Phases 1–3 implemented (2026-07)**, prompted by real Editor feedback that streetlamps rendered as "big opaque balls of light... turning the playfield white." `CityLightingProfile` (real-light budget/intensity/range, emissive base brightness, night boost ceiling, night ambient/bloom, flicker/buzz/chase timing) replaces every hardcoded lighting constant; `GlowPointRegistry`/`DynamicLightBudget` generalize the streetlamp-only budget into one shared pool any light kind competes for; `EmissiveAnimator` + `EmissiveAnimatorDriver` add Steady/Flicker/Buzz/Chase behaviors, one batched Update() for the whole city. Wired end-to-end: apartment windows flicker (`BuildingDresser.WindowGlow`, ~2-in-5 floors lit), the movie palace's neon buzzes, its marquee grew a 10-bulb chaser row. Office-tower windows (no per-floor geometry yet) and true individual window-pane granularity remain deferred. **Follow-up (2026-07): traffic headlights + brake lights, the system's first MOVING consumer.** `TrafficCar` cars now carry forward-and-slightly-down headlights (Tier 1 emissive bulb always; Tier 2 a real budgeted `Spot` `Light` competing in the SAME shared `DynamicLightBudget` pool every streetlamp/window/neon already does — nearest-to-camera wins, so a far/off-screen car's headlight never costs a real light, the literal "only render the ones in regions the user can see" ask, with no new perf-management code) and Tier-1-only red brake lights (toggled by a frame-to-frame speed-drop signal, gated to driving + night). Required two small, backward-compatible extensions to `GlowPointRegistry`/`DynamicLightBudget`, both defaulting to today's exact prior behavior for every existing streetlamp/window/neon call site: `spotAimsWithTransform` (a registered point's own live world rotation drives the promoted Light's aim, instead of the shared hardcoded straight-down constant — a car's beam has to track wherever it's currently facing, not the road below it) and `isEligible` (an optional per-point predicate checked before a point is even a budget candidate — a parked or daylight car's headlight simply never competes for or holds a slot, without needing true register/unregister lifecycle on an otherwise append-only registry). Headlight/brake materials are shared across the whole fleet and registered with the existing `NeonRegistry` boost pipeline once, at first mint, so brightness tracks day/night the same way every other emissive prop already does. Verified via a real `dotnet build` stub-compile of the actual `DynamicLightBudget.cs`/`TrafficCar.cs`/`ShaderUtil.cs`/`NeonRegistry.cs`/`DayNightState.cs` files together against the real citygen-core DLL plus shape stubs for `RuntimeCityBuilder`/`RoadDresser`/`MonsterAgent`/`CityLightingProfile` — 0 errors/warnings. Not visually verified — flightcheck-compiled only |

**Status legend**: *Draft* (numbers are v0.1 proposals) → *Reviewed* (survived Phase-0 paper playtest and a read-through) → *Locked* (implementation depends on it; changes require a decision-log entry in [12](12-open-questions.md)).

## Conventions

- **Cross-references** are relative links; the genome schema in [06](06-mutator-design.md) is *normative* — docs 04, 07, and 08 consume it and must be updated together with it.
- **Tuning numbers** are tagged "v0.1" and consolidated in per-doc tuning tables; they are proposals to be validated in the Phase-0/1 playtests ([11-roadmap.md](11-roadmap.md)), not commitments.
- Terms below are defined **only here**; other docs link rather than redefine.

## Glossary

| Term | Definition |
| --- | --- |
| **Affinity** | A monster's Lumen coupling gene: `solar`, `lunar`, or `neutral`. Decides aura buffs by phase ([03](03-mana-system.md)) |
| **Aggression** | A Citizen's 0–1 fight/flee propensity, rolled independently of age and body type ([19](19-citizens.md)) |
| **Allele** | One part slot's gene triple `{partFamilyId, sizeGene, variantGene}` ([06](06-mutator-design.md)) |
| **Archetype / body plan** | One of 6 base skeletons (biped, quadruped, serpentine, winged, hulking, amorphous); selects rig, animation set, and slot list |
| **Aura** | The 3-hex radius around an emitter where affinity modifiers apply ([03](03-mana-system.md)) |
| **Blood Bank** | A player-built, Ghoul-constructed storage structure: refills nearby friendly units' onboard blood and extends the Blood wallet cap; distinct from doc 17's hospital world-source node ([22](22-economy-system.md)) |
| **Bone Pile** | A player-built storage structure: refills onboard bone (armour stock) and extends the Bones wallet cap ([22](22-economy-system.md)) |
| **Brain Trust** | A player-built storage structure: refills onboard grey-matter charge and extends the greyMatter wallet cap ([22](22-economy-system.md)) |
| **Brain budget** | The stat-point and quirk cap imposed by a monster's brain quality (Dim/Average/Gifted/Mastermind) ([06](06-mutator-design.md)) |
| **Brains** | Player-facing term for two things: the discrete, one-per-monster Brain tier-item that gates the genome's stat budget (see **Brain budget**, above); and the bulk, counted resource harvested from Citizens and vanquished foes, individually weak, whose only sink is Megabrain Augmentation — internally tracked as the distinct field `greyMatter` so the two never collide in code ([05](05-component-economy.md), [20](20-harvest-and-repair.md)) |
| **Cannibalize** | Retire one of your own genomes at the Workshop, or recall a living fielded creature at the Vat mid-match, converting it back into Bones/Body Parts/a Brain-tier roll at 50% of its build cost — the same operation in two contexts ([06](06-mutator-design.md), [20](20-harvest-and-repair.md)) |
| **Citizen** | A non-combatant city NPC — age, body type, and personality generated, never bred; distinct from the Human Army faction ([17](17-factions.md)) ([19](19-citizens.md)) |
| **Collection Station** | A capturable structure (captures like an emitter) that converts Citizen deaths within its radius into banked Blood/Bones/Brains for its controller; one per Community Hub ([18](18-city-battlefields.md), [20](20-harvest-and-repair.md)) |
| **Community Hub** | A high-population landmark building type — hospital, school, or old-age home — generated at 4× standard Citizen density ([18](18-city-battlefields.md), [19](19-citizens.md), [20](20-harvest-and-repair.md)) |
| **Commute mode** | Offline-queued Mutator operations from the mobile lab; results delivered by push ([07](07-mutator-server-architecture.md)) |
| **Components** | The four material resources: **Blood** (upkeep), **Bones** (structure), **Body Parts** (capability + feedstock), **Brains** (gating) ([05](05-component-economy.md)) |
| **Distant skyline** | The outermost engagement-zone LOD tier: pure visual backdrop beyond ~1 km of any live engagement, no simulation ([18](18-city-battlefields.md)) |
| **Dominion** | Victory by holding ≥60% of emitters for one full Lumen Cycle ([02](02-gameplay-overview.md)) |
| **Emitter** | A capturable map point that banks mana to its owner at a rate set by its polarity and the current phase ([03](03-mana-system.md)) |
| **Engagement zone** | The ~150–200 m radius around live combat that runs full server-authoritative simulation on a city battlefield; the existing ~60-entity budget applies per-zone, not per-map ([18](18-city-battlefields.md)) |
| **Failed experiment** | The comic, fiction-friendly result of an invalid Mutator operation — never silent clamping ([06](06-mutator-design.md)) |
| **Genome** | The complete, immutable, server-signed description of a creature (~200–400 B). The normative schema lives in [06](06-mutator-design.md) |
| **Genome fragment** | A salvage drop that reveals one enemy part family to your catalog ([04](04-combat-model.md)) |
| **Ghoul** | The gatherer unit class: auto-scavenges corpse salvage and blood spills into the wallet, and constructs storage structures and the Stitchworks; unarmed, flees when threatened ([22](22-economy-system.md)) |
| **Graft** | The deterministic Mutator operator: pay parts to set a slot directly ([06](06-mutator-design.md)) |
| **Local city** | The mid engagement-zone LOD tier (~1 km): buildings static, Citizens run as client-side crowd only ([18](18-city-battlefields.md)) |
| **Lumen Cycle** | The 4-minute Day→Dusk→Night→Dawn match clock driving emitter output and affinity buffs ([03](03-mana-system.md)) |
| **Mana** | The *energy* currency, earned only from emitters; pays reanimation surges and abilities. Distinct from components (*material*) ([03](03-mana-system.md)) |
| **Megabrain Augmentation** | A Mastermind-only, one-time Mutator operation: 100 harvested Brains for a flat +7.2 Capacity bonus — the doctors' "mind control" fiction: one swollen brain possessing a 40-strong platoon ([06](06-mutator-design.md), [16](16-brains-behavior-command.md)) |
| **Menagerie** | The ≤12 active monster designs you can reanimate in a match ([02](02-gameplay-overview.md)) |
| **Moon dial** | The always-public HUD clock showing the current Lumen phase and the 10-second transition warning |
| **Mutate** | The one-parent Mutator operator: biased random mutation, steered by fed components ([06](06-mutator-design.md)) |
| **Onboard pools** | The three per-unit resource tanks — blood (fuel), bone (armour stock), brain (grey-matter charge) — with genome-derived capacities; drained by fighting and damage, refilled by eating/storage/medics, degrading efficiency toward floors when empty ([22](22-economy-system.md)) |
| **The Mutator** | The server-side genetic-algorithm laboratory: Mutate, Splice, Graft ([06](06-mutator-design.md), [07](07-mutator-server-architecture.md)) |
| **The Notebook** | Dr. Frankenstein's inherited journals — the game's unifying UI metaphor and your collection record ([01](01-vision.md)) |
| **Part family** | A catalog entry of part meshes (claw-arm, bat-wing…) with slot/archetype compatibility and authored scale bounds ([08](08-creature-visualization.md)) |
| **Power budget** | A genome's normalized stat-point sum; capped by brain quality, read by matchmaking ([06](06-mutator-design.md), [09](09-multiplayer-architecture.md)) |
| **Quirk** | A minor rule-bending genome trait (regeneration, fear aura…); max 2 per monster ([06](06-mutator-design.md)) |
| **Reanimation** | Fielding a Menagerie design at the Vat: component bill + mana surge + 5–20 s build time ([05](05-component-economy.md)) |
| **Repair** | An in-match Vat command that restores a living creature's lost HP for a Bone+Blood cost scaled to missing HP; distinct from surgery/graft and the `regeneration` quirk ([20](20-harvest-and-repair.md)) |
| **Salvage** | The 40–60% component drop on a monster's death hex, lootable by either side for 15 s ([04](04-combat-model.md)) |
| **Sawbones** | The medic unit class: auto-triage AI, channels field Repair at half Vat speed and transfuses onboard pools from the wallet; one medic per patient, no attack ([22](22-economy-system.md)) |
| **Splice** | The two-parent Mutator operator: crossover breeding, with low-odds cross-archetype hybrids ([06](06-mutator-design.md)) |
| **Stitchworks** | A player-built forward factory: reanimates Menagerie designs at 1.5× Vat build time with a 5-deep queue, and channels match-scoped field augments (+50% to an onboard capacity) ([22](22-economy-system.md)) |
| **Structure HP** | A building's Vitality-equivalent stat, resolved through the existing damage formula ([04](04-combat-model.md), [18](18-city-battlefields.md)) |
| **Style preset** | An authored parameter set (road layout, density, facade/prop kit) skinning the shared city generator per theme ([18](18-city-battlefields.md)) |
| **Territory tick** | The per-controlled-hex blood trickle (+0.1 blood/min) ([05](05-component-economy.md)) |
| **Vat** | Your base: the reanimation point and the destroy-to-win target ([02](02-gameplay-overview.md)) |
| **The Workshop** | The resource-facing section of the Lab: build a monster from Bones/Parts/Brains/Blood, priced live as you build. Its tools are Mutate, Splice, Graft, Megabrain Augmentation, and Cannibalize ([06](06-mutator-design.md)) |
